<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Onboard3 — Learn. Earn. Build.</title>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-green: #39FF14;
            --dark-green: #00A86B;
            --bg-dark: #0a0a0a;
            --bg-darker: #050505;
            --white: #ffffff;
            --grey: #888888;
        }

        body {
            font-family: 'Space Grotesk', sans-serif;
            background: var(--bg-dark);
            color: var(--white);
            overflow-x: hidden;
            line-height: 1.6;
        }

        /* Smooth Scroll */
        html {
            scroll-behavior: smooth;
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(10, 10, 10, 0.9);
            backdrop-filter: blur(10px);
            z-index: 1000;
            padding: 1.5rem 5%;
            border-bottom: 1px solid rgba(57, 255, 20, 0.1);
        }

        nav .container {
            max-width: 1400px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 800;
            background: linear-gradient(135deg, var(--primary-green), var(--dark-green));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        .nav-links a {
            color: var(--white);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s;
        }

        .nav-links a:hover {
            color: var(--primary-green);
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            padding: 2rem 5%;
            margin-top: 80px;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 50%, rgba(57, 255, 20, 0.15) 0%, transparent 50%),
                radial-gradient(circle at 80% 50%, rgba(0, 168, 107, 0.15) 0%, transparent 50%);
            z-index: -1;
        }

        .hero-content {
            max-width: 1400px;
            text-align: center;
            z-index: 1;
        }

        .hero h1 {
            font-size: clamp(3rem, 8vw, 6rem);
            font-weight: 800;
            margin-bottom: 1.5rem;
            background: linear-gradient(135deg, var(--primary-green), var(--white), var(--dark-green));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: fadeInUp 1s ease;
        }

        .hero p {
            font-size: clamp(1.2rem, 3vw, 1.8rem);
            color: var(--grey);
            margin-bottom: 3rem;
            animation: fadeInUp 1s ease 0.2s backwards;
        }

        .cta-buttons {
            display: flex;
            gap: 1.5rem;
            justify-content: center;
            flex-wrap: wrap;
            animation: fadeInUp 1s ease 0.4s backwards;
        }

        .btn {
            padding: 1rem 2.5rem;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            display: inline-block;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--primary-green), var(--dark-green));
            color: var(--bg-dark);
            border: none;
        }

        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 30px rgba(57, 255, 20, 0.3);
        }

        .btn-secondary {
            background: transparent;
            color: var(--white);
            border: 2px solid var(--primary-green);
        }

        .btn-secondary:hover {
            background: rgba(57, 255, 20, 0.1);
        }

        /* Section Styles */
        section {
            padding: 6rem 5%;
            max-width: 1400px;
            margin: 0 auto;
        }

        .section-title {
            font-size: clamp(2.5rem, 5vw, 3.5rem);
            font-weight: 800;
            margin-bottom: 3rem;
            text-align: center;
            background: linear-gradient(135deg, var(--primary-green), var(--white));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        /* Mission Section */
        .mission-content {
            max-width: 900px;
            margin: 0 auto;
            text-align: center;
            font-size: 1.5rem;
            line-height: 2;
            color: var(--grey);
        }

        /* Problem & Solution */
        .problem-solution {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 3rem;
            margin-top: 3rem;
        }

        .card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 2.5rem;
            border: 1px solid rgba(57, 255, 20, 0.1);
            transition: all 0.3s;
        }

        .card:hover {
            transform: translateY(-10px);
            border-color: rgba(57, 255, 20, 0.3);
            box-shadow: 0 20px 40px rgba(57, 255, 20, 0.1);
        }

        .card h3 {
            font-size: 1.8rem;
            margin-bottom: 1.5rem;
            color: var(--primary-green);
        }

        .card ul {
            list-style: none;
            color: var(--grey);
        }

        .card li {
            margin-bottom: 1rem;
            padding-left: 1.5rem;
            position: relative;
        }

        .card li::before {
            content: '→';
            position: absolute;
            left: 0;
            color: var(--primary-green);
        }

        /* Roadmap */
        .roadmap-timeline {
            position: relative;
            padding: 2rem 0;
        }

        .roadmap-item {
            display: grid;
            grid-template-columns: 200px 1fr;
            gap: 3rem;
            margin-bottom: 3rem;
            opacity: 0;
            transform: translateX(-50px);
            animation: slideIn 0.8s forwards;
        }

        .roadmap-item:nth-child(1) { animation-delay: 0.1s; }
        .roadmap-item:nth-child(2) { animation-delay: 0.2s; }
        .roadmap-item:nth-child(3) { animation-delay: 0.3s; }
        .roadmap-item:nth-child(4) { animation-delay: 0.4s; }
        .roadmap-item:nth-child(5) { animation-delay: 0.5s; }
        .roadmap-item:nth-child(6) { animation-delay: 0.6s; }

        .roadmap-date {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary-green);
        }

        .roadmap-content {
            background: rgba(255, 255, 255, 0.03);
            padding: 1.5rem;
            border-radius: 15px;
            border-left: 3px solid var(--primary-green);
        }

        .roadmap-content h4 {
            margin-bottom: 0.5rem;
            font-size: 1.3rem;
        }

        /* Traction */
        .traction-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .stat-card {
            text-align: center;
            padding: 2rem;
            background: rgba(57, 255, 20, 0.05);
            border-radius: 20px;
            border: 1px solid rgba(57, 255, 20, 0.2);
            transition: all 0.3s;
        }

        .stat-card:hover {
            transform: scale(1.05);
            background: rgba(57, 255, 20, 0.1);
        }

        .stat-number {
            font-size: 3rem;
            font-weight: 800;
            color: var(--primary-green);
            display: block;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            color: var(--grey);
            font-size: 1.1rem;
        }

        /* Revenue */
        .revenue-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
            margin-top: 3rem;
        }

        .revenue-card {
            background: linear-gradient(135deg, rgba(57, 255, 20, 0.1), rgba(0, 168, 107, 0.05));
            padding: 2rem;
            border-radius: 20px;
            border: 1px solid rgba(57, 255, 20, 0.2);
        }

        .revenue-card h4 {
            font-size: 1.4rem;
            margin-bottom: 1rem;
            color: var(--primary-green);
        }

        /* Funding */
        .funding-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            margin-top: 3rem;
        }

        .funding-info h3 {
            font-size: 2.5rem;
            color: var(--primary-green);
            margin-bottom: 2rem;
        }

        .allocation-list {
            list-style: none;
        }

        .allocation-list li {
            padding: 1rem;
            margin-bottom: 1rem;
            background: rgba(255, 255, 255, 0.03);
            border-radius: 10px;
            border-left: 3px solid var(--primary-green);
        }

        .allocation-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .percentage {
            font-weight: 700;
            color: var(--primary-green);
            font-size: 1.3rem;
        }

        /* Team */
        .team-content {
            max-width: 800px;
            margin: 3rem auto;
            text-align: center;
        }

        .founder-card {
            background: linear-gradient(135deg, rgba(57, 255, 20, 0.1), rgba(0, 168, 107, 0.05));
            padding: 3rem;
            border-radius: 20px;
            border: 2px solid rgba(57, 255, 20, 0.3);
            margin-bottom: 2rem;
        }

        .founder-name {
            font-size: 2rem;
            color: var(--primary-green);
            margin-bottom: 0.5rem;
        }

        .founder-title {
            font-size: 1.2rem;
            color: var(--grey);
            margin-bottom: 1rem;
        }

        /* Footer CTA */
        .footer-cta {
            text-align: center;
            padding: 6rem 5%;
            background: linear-gradient(135deg, rgba(57, 255, 20, 0.1), rgba(0, 168, 107, 0.05));
        }

        .footer-cta h2 {
            font-size: clamp(2rem, 4vw, 3rem);
            margin-bottom: 2rem;
            line-height: 1.4;
        }

        .social-links {
            display: flex;
            gap: 2rem;
            justify-content: center;
            margin-top: 2rem;
            flex-wrap: wrap;
        }

        .social-links a {
            color: var(--primary-green);
            text-decoration: none;
            font-size: 1.2rem;
            font-weight: 600;
            transition: all 0.3s;
        }

        .social-links a:hover {
            transform: translateY(-3px);
            color: var(--white);
        }

        /* Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideIn {
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }

        /* Responsive */
        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            .roadmap-item {
                grid-template-columns: 1fr;
                gap: 1rem;
            }

            .funding-content {
                grid-template-columns: 1fr;
            }

            section {
                padding: 4rem 5%;
            }
        }

        /* Scroll Animation Observer */
        .fade-in {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.8s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>
   

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <h1>Learn. Earn. Build.</h1>
            <p>Onboarding the next 100,000 Africans into Web3</p>
            <div class="cta-buttons">
                <a href="#" class="btn btn-secondary">View Pitch Deck</a>
            </div>
        </div>
    </section>

    <!-- Mission Section -->
    <section id="mission" class="fade-in">
        <h2 class="section-title">Our Mission</h2>
        <div class="mission-content">
            <p>Empowering Africans with <strong style="color: var(--primary-green);">real skills</strong>, <strong style="color: var(--primary-green);">community</strong>, and <strong style="color: var(--primary-green);">opportunities</strong> in Web3.</p>
        </div>
    </section>

    <!-- Problem & Solution -->
    <section id="problem-solution" class="fade-in">
        <h2 class="section-title">The Challenge & Our Solution</h2>
        <div class="problem-solution">
            <div class="card">
                <h3>The Problem</h3>
                <ul>
                    <li>High unemployment in Nigeria (5.3% but 42.5% youth)</li>
                    <li>Lack of accessible tech education</li>
                    <li>Web3 seen as intimidating or scams</li>
                    <li>No clear pathways to earning</li>
                    <li>Brain drain & limited opportunities</li>
                </ul>
            </div>
            <div class="card">
                <h3>Our Solution</h3>
                <ul>
                    <li>Free, structured Web3 learning paths</li>
                    <li>Earn-while-you-learn model (quests, bounties)</li>
                    <li>Local community & mentorship</li>
                    <li>IRL events & networking</li>
                    <li>Job board & partnerships</li>
                </ul>
            </div>
        </div>
    </section>

    <!-- Roadmap -->
    <section id="roadmap" class="fade-in">
        <h2 class="section-title">Our Roadmap</h2>
        <div class="roadmap-timeline">
            <div class="roadmap-item">
                <div class="roadmap-date">Nov 2025</div>
                <div class="roadmap-content">
                    <h4>Launch Virtual Onboarding</h4>
                    <p>100 participants, structured learning modules</p>
                </div>
            </div>
            <div class="roadmap-item">
                <div class="roadmap-date">Dec 2025</div>
                <div class="roadmap-content">
                    <h4>Quest & Bounty Platform</h4>
                    <p>Live platform, sponsored tasks, earning opportunities</p>
                </div>
            </div>
            <div class="roadmap-item">
                <div class="roadmap-date">Dec 2026</div>
                <div class="roadmap-content">
                    <h4>First IRL Event</h4>
                    <p>Kwara meetup, networking, workshops</p>
                </div>
            </div>
            <div class="roadmap-item">
                <div class="roadmap-date">Feb 2026</div>
                <div class="roadmap-content">
                    <h4>Partnership Expansion</h4>
                    <p>Web3 projects, sponsors, mentors</p>
                </div>
            </div>
            <div class="roadmap-item">
                <div class="roadmap-date">Mar 2026</div>
                <div class="roadmap-content">
                    <h4>Job Board Launch</h4>
                    <p>Connect builders with opportunities</p>
                </div>
            </div>
            <div class="roadmap-item">
                <div class="roadmap-date">Apr 2026</div>
                <div class="roadmap-content">
                    <h4>Scale to 10,000 Users</h4>
                    <p>Multiple cities, regional expansion</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Traction -->
    <section id="traction" class="fade-in">
        <h2 class="section-title">Early Traction</h2>
        <div class="traction-grid">
            <div class="stat-card">
                <span class="stat-number">300+</span>
                <div class="stat-label">X (Twitter) Followers</div>
            </div>
            <div class="stat-card">
                <span class="stat-number">120+</span>
                <div class="stat-label">Telegram Members</div>
            </div>
            <div class="stat-card">
                <span class="stat-number">50%</span>
                <div class="stat-label">Website Ready</div>
            </div>
            <div class="stat-card">
                <span class="stat-number">Nov 2025</span>
                <div class="stat-label">Virtual Onboarding Launch</div>
            </div>
        </div>
    </section>

    <!-- Market Opportunity -->
    <section id="market" class="fade-in">
        <h2 class="section-title">Market Opportunity</h2>
        <div class="problem-solution">
            <div class="card">
                <h3>🌍 Africa's Web3 Growth</h3>
                <ul>
                    <li>Nigeria leads Africa in crypto adoption</li>
                    <li>220M population, 70% under 30</li>
                    <li>Growing internet penetration</li>
                    <li>High demand for alternative income</li>
                    <li>Untapped talent pool</li>
                </ul>
            </div>
            <div class="card">
                <h3>📈 Our Target</h3>
                <ul>
                    <li>100,000 users in 3 years</li>
                    <li>Focus: Nigeria, Ghana, Kenya</li>
                    <li>Ages 18-35, tech-curious</li>
                    <li>Students, freelancers, builders</li>
                    <li>Underserved communities</li>
                </ul>
            </div>
        </div>
    </section>

    <!-- Revenue -->
    <section id="revenue" class="fade-in">
        <h2 class="section-title">Revenue Streams</h2>
        <div class="revenue-grid">
            <div class="revenue-card">
                <h4>💰 Sponsored Onboarding</h4>
                <p>Web3 projects pay to sponsor cohorts and reach our community</p>
            </div>
            <div class="revenue-card">
                <h4>🎯 Branded Quests</h4>
                <p>Companies sponsor tasks and bounties for user engagement</p>
            </div>
            <div class="revenue-card">
                <h4>🎟️ IRL Events</h4>
                <p>Ticketed workshops, hackathons, and networking events</p>
            </div>
            <div class="revenue-card">
                <h4>🤝 Affiliate Programs</h4>
                <p>Partnerships with Web3 platforms and tools</p>
            </div>
        </div>
    </section>

    <!-- Funding -->
    <section id="funding" class="fade-in">
        <h2 class="section-title">Pre-Seed Funding</h2>
        <div class="funding-content">
            <div class="funding-info">
                <h3>$20K – $25K</h3>
                <p style="color: var(--grey); margin-bottom: 2rem;">Capital to scale operations, launch platform, and host events</p>
                <ul class="allocation-list">
                    <li>
                        <div class="allocation-item">
                            <span>Platform Development</span>
                            <span class="percentage">40%</span>
                        </div>
                    </li>
                    <li>
                        <div class="allocation-item">
                            <span>Marketing & Growth</span>
                            <span class="percentage">25%</span>
                        </div>
                    </li>
                    <li>
                        <div class="allocation-item">
                            <span>IRL Events</span>
                            <span class="percentage">20%</span>
                        </div>
                    </li>
                    <li>
                        <div class="allocation-item">
                            <span>Operations</span>
                            <span class="percentage">15%</span>
                        </div>
                    </li>
                </ul>
            </div>
            <div class="card">
                <h3>Why Now?</h3>
                <ul>
                    <li>Community momentum is building</li>
                    <li>Platform 50% complete</li>
                    <li>First cohort starting November</li>
                    <li>Early partnerships in discussion</li>
                    <li>Perfect timing for pre-seed</li>
                </ul>
            </div>
        </div>
    </section>

    <!-- Team -->
    <section id="team" class="fade-in">
        <h2 class="section-title">The Team</h2>
        <div class="team-content">
            <div class="founder-card">
                <div class="founder-name">Beebrain</div>
                <div class="founder-title">Founder & CEO</div>
                <p style="color: var(--grey);">Developer, Product Manager & Community Lead driving Onboard3's vision to onboard 100,000 Africans into Web3</p>
            </div>
            <div class="card">
                <h3>Growing Team</h3>
                <p style="color: var(--grey); margin-bottom: 1rem;">Core volunteers supporting operations</p>
                <p style="color: var(--primary-green); font-weight: 600;">Open Roles: Community Managers, Content Creators, Developers</p>
            </div>
        </div>
    </section>

    <!-- Footer CTA -->
    <section class="footer-cta">
        <h2>"We're not just onboarding users — we're building the next generation of African Web3 builders."</h2>
        <div class="cta-buttons">
            <a href="#" class="btn btn-primary">Join Onboard3</a>
        </div>
        <div class="social-links">
            <a href="https://twitter.com/Onboard3__" target="_blank">@Onboard3__ (X)</a>
            <a href="mailto:hello@onboard3.xyz">hello@onboard3.xyz</a>
        </div>
        <p style="margin-top: 3rem; color: var(--grey);">© 2025 Onboard3. Building Africa's Web3 Future.</p>
    </section>

    <script>
        // Intersection Observer for scroll animations
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-in').forEach(el => observer.observe(el));

        // Smooth scroll for nav links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });
    </script>
</body>
</html>